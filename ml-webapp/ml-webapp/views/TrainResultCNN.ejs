<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Model Training Results</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body class="container mt-5">

    <h1 class="text-center">Model Training Results</h1>

    <% if (message) { %>
        <div class="alert alert-success">
            <h4><%= message %></h4>
        </div>
    <% } else { %>
        <div class="alert alert-danger">
            <h4>Training Failed</h4>
        </div>
    <% } %>

    <table class="table table-bordered mt-4">
        <tbody>
            <tr>
                <th>Model Path</th>
                <td><%= modelPath ? modelPath : "N/A" %></td>
            </tr>
            <tr>
                <th>Final Training Loss</th>
                <td><%= finalLoss !== null ? finalLoss.toFixed(4) : "N/A" %></td>
            </tr>
            <tr>
                <th>Epochs Trained</th>
                <td><%= epochs !== null ? epochs : "N/A" %></td>
            </tr>
        </tbody>
    </table>

    <div class="text-center mt-4">
        <a href="/" class="btn btn-primary">Back to Home</a>
    </div>

</body>
</html>
